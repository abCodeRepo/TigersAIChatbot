<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TigersAI Chatbot</title>
    <link rel="icon" type="image/x-icon" href="/assets/img/tiger2.svg">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/fullcalendar@5.10.2/main.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

    <div class="navbar">
        <!-- Navbar brand and links -->
        <div class="brand">
            <a href="#">TigersAI</a>
            <!-- Sidebar toggle button -->
            <button class="sidebar-toggler" aria-label="Toggle sidebar">☰</button>
        </div>

        <ul class="nav">
            <li class="nav-item">
                <a class="nav-link disabled nav-welcome-text" aria-disabled="true">Welcome, <%= user.username %>!</a>
            </li>
            <li class="nav-item">
                <form action="/logout" method="POST">
                    <button type="submit" class="logout-button">Logout</button>
                </form>
            </li>
        </ul>

    </div>
    
    <!-- Sidebar menu -->
    <div class="sidebar">
        <ul>
            <li><a href="#">Chatbot</a></li>
            <li><a href="/subnetcalculator">Subnet Calculator</a></li>
        </ul>
    </div>
    
    <div class="container container-margin">
        
        <!-- Chat and Calendar Container -->
        <div id="chat-calendar-container">
            <!-- Chat Container -->
            <div id="chat-container">
                <div id="chat-box">
                    <div id="messages"></div>
                </div>
                <div id="user-input-container">
                    <input type="text" id="user-message" placeholder="Type a message..." />
                    <button id="send-button">Send</button>
                </div>
            </div>

            <!-- Calendar Container -->
            <div id="calendar-container">
                <h2>Conversation History Calendar</h2>
                <% if (user.role === 'teacher' || 'admin') { %>
                    <div id="user-filter-container" style="display: none;">
                        <label for="user-filter">Select a User:</label>
                        <select id="user-filter">
                            <option value="">-- No Selection --</option>
                        </select>
                    </div>
                <% } %>
                
                
                <div id="calendar"></div>
            </div>
        </div>
    </div>

    <div id="footer"></div>
    <script src="https://cdn.jsdelivr.net/npm/fullcalendar@5.10.2/main.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
    <script>
        window.userRole = "<%= user.role %>";
        window.userId = "<%= user.id %>"
    </script>
    <script src="/scripts/script.js"></script>

    
</html>
